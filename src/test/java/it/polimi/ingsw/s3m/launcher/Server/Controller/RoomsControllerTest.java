package it.polimi.ingsw.s3m.launcher.Server.Controller;

class RoomsControllerTest{

	/*
	@Test
	void loginMultipleClientsWithTheSameNickname(){
		ClientHandler client1 = new ClientHandler(null);
		LoginMessage login1 = new LoginMessage("test");
		ClientHandler client2 = new ClientHandler(null);
		LoginMessage login2 = new LoginMessage("test");

		LoginMessage loginResult1 = RoomsController.instance().loginClient(client1, login1);
		LoginMessage loginResult2 = RoomsController.instance().loginClient(client2, login2);

		assertTrue(loginResult1.isSuccessful());
		assertFalse(loginResult2.isSuccessful());
	}
	*/
}